'use client';\nimport React from 'react';\nexport type Messages = Record<string,string>;\nconst Ctx = React.createContext<{ locale:string; t:(k:string)=>string }|null>(null);\nexport function I18nProvider({ locale, messages, children }:{ locale:string; messages:Messages; children:React.ReactNode }){ const t = React.useCallback((k:string)=>messages[k]??k,[messages]); return <Ctx.Provider value={{ locale, t }}>{children}</Ctx.Provider>; }\nexport function useI18n(){ const v = React.useContext(Ctx); if(!v) return { locale:'en', t:(k:string)=>k }; return v; }
