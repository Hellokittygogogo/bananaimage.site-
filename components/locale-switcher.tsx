'use client';\nimport { useEffect, useState } from 'react';\nimport { Button } from './ui/button';\nfunction setCookie(name:string,value:string){ document.cookie = ${name}=; path=/; max-age=; }\nexport default function LocaleSwitcher(){ const [locale,setLocale]=useState('en'); useEffect(()=>{ const m=document.cookie.match(/(?:^|;\s*)NEXT_LOCALE=([^;]+)/); setLocale(m?.[1]||'en'); },[]); const toggle=()=>{ const next = locale==='en'?'zh':'en'; setCookie('NEXT_LOCALE', next); location.reload(); }; return <Button size='sm' variant='ghost' onClick={toggle}>{locale==='en'?'中文':'EN'}</Button>; }
